{
  "name": "steam-play-time",
  "private": true,
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "cd backend && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "install:frontend": "npm --prefix frontend install"
  }
}
