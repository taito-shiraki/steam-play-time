# Steam プレイ時間ビジュアライザー 製品仕様書

**バージョン:** 1.2
**作成日:** 2026-04-09
**最終更新日:** 2026-04-11
**ステータス:** Approved
**ベースドキュメント:** DESIGN_STEAM_TRACKER.md v1.0

---

## 1. 製品概要

### 1.1 サービス名（仮）
SteamStats / Playlog / GameClock（未定 -- 要確定）

### 1.2 コンセプト
Steamユーザーのプレイ時間データを取得し、わかりやすいグラフと統計で可視化するWebサービス。
「自分がどのゲームにどれだけ人生を使ったか、一発でわかる」体験を提供する。

### 1.3 ターゲットユーザー
1. Steam所持ゲームが多く、プレイ傾向を振り返りたいゲーマー
2. ゲーム費用対効果（コスパ）を把握したい節約志向のゲーマー
3. 年間プレイ統計を友人やSNSでシェアしたい人
4. ゲーム予算を管理したい人
5. 友人とプレイ傾向を比較して楽しみたい人

### 1.4 解決する課題
- Steam標準のプロフィールページではグラフ表示が貧弱で、プレイ傾向を直感的に把握できない
- 購入価格に対するプレイ時間の費用対効果を確認する手段がない
- プレイ統計をSNSで気軽にシェアする手段がない
- 日本語対応のSteam統計サービスがほぼ存在しない

### 1.5 技術スタック（設計書より）
| レイヤー | 技術 |
|---------|------|
| フロントエンド | React + Vite |
| グラフ描画 | Recharts または Chart.js |
| スタイリング | Tailwind CSS |
| バックエンド | Python FastAPI |
| ホスティング(FE) | Vercel |
| ホスティング(BE) | Railway または Render |
| キャッシュ | Redis またはメモリキャッシュ |
| DB (Phase2以降) | PostgreSQL (Supabase) |

---

## 2. 機能一覧

### F-001: Steam ID 入力フォーム
- **機能名:** Steam ID 入力
- **ユーザーストーリー:** ゲーマーとして、SteamID64またはプロフィールURLを入力して自分のプレイデータを取得したい
- **受け入れ基準:**
  1. SteamID64（17桁数値）の直接入力を受け付ける
  2. Steamプロフィールのカスタム URL（https://steamcommunity.com/id/xxx）を受け付け、バックエンドでSteamID64に変換する
  3. 入力が不正な場合、具体的なエラーメッセージを表示する
  4. 入力中にローディング状態を表示する
- **優先度:** Must

### F-002: プレイ時間ランキング表示
- **機能名:** プレイ時間ランキング（TOP20 棒グラフ）
- **ユーザーストーリー:** ゲーマーとして、自分が最もプレイしたゲームTOP20を棒グラフで一目で確認したい
- **受け入れ基準:**
  1. 総プレイ時間の多い順にTOP20のゲームを棒グラフで表示する
  2. 各バーにゲーム名とプレイ時間（時間単位）を表示する
  3. ゲームアイコンをバーまたはラベルに併せて表示する
- **優先度:** Must

### F-003: 直近2週間のプレイ比較グラフ
- **機能名:** 直近2週間のプレイ状況
- **ユーザーストーリー:** ゲーマーとして、直近2週間にプレイしたゲームの時間配分を確認したい
- **受け入れ基準:**
  1. Steam APIの playtime_2weeks データをもとに横棒グラフを表示する
  2. 直近2週間にプレイしていないゲームは表示しない
  3. 直近2週間のプレイがゼロの場合、「最近プレイしたゲームがありません」と表示する
- **優先度:** Must

### F-004: 総合統計サマリー
- **機能名:** 総合統計サマリーカード
- **ユーザーストーリー:** ゲーマーとして、自分の総ゲーム数・総プレイ時間・最多プレイタイトルをひと目で把握したい
- **受け入れ基準:**
  1. 総所持ゲーム数を表示する
  2. 全ゲームの総プレイ時間（時間単位）を表示する
  3. 最もプレイ時間の長いゲーム名とその時間を表示する
  4. ユーザーのアバター画像とプレイヤー名を表示する
- **優先度:** Must

### F-005: SNSシェア機能
- **機能名:** Twitter/X シェアボタン
- **ユーザーストーリー:** ゲーマーとして、自分のプレイ統計をTwitter/Xにワンタップでシェアしたい
- **受け入れ基準:**
  1. 結果ページにTwitter/Xシェアボタンを設置する
  2. シェアテキストに総プレイ時間・最多プレイゲーム名を含める
  3. シェア時にサービスURLを自動付与する
- **優先度:** Must

### F-006: プロフィール非公開時のエラーハンドリング
- **機能名:** プロフィール非公開エラー案内
- **ユーザーストーリー:** プロフィールを非公開にしているユーザーとして、なぜデータが表示されないか理解し、公開設定の手順を知りたい
- **受け入れ基準:**
  1. プロフィール非公開の場合、専用のエラーメッセージを表示する
  2. Steam側のプロフィール公開設定手順をステップ形式で案内する
  3. Steam設定ページへのリンクを提供する
- **優先度:** Must

### F-007: ダークテーマUI
- **機能名:** ダークテーマデザイン
- **ユーザーストーリー:** ゲーマーとして、Steamの雰囲気に合ったダークテーマのUIでサービスを利用したい
- **受け入れ基準:**
  1. デフォルトのカラースキームをダークテーマとする
  2. Steam風のカラーパレット（暗い背景に明るいアクセント）を採用する
  3. テキスト・グラフの視認性をダーク背景上で確保する
- **優先度:** Must

### F-008: レスポンシブ対応
- **機能名:** モバイルレスポンシブUI
- **ユーザーストーリー:** スマホユーザーとして、モバイル端末でもグラフや統計を快適に閲覧したい
- **受け入れ基準:**
  1. 主要ブレークポイント（モバイル / タブレット / デスクトップ）でレイアウトが適切に切り替わる
  2. グラフがモバイル画面幅に収まり、横スクロールなしで閲覧できる
  3. タッチ操作でグラフの詳細値を確認できる
- **優先度:** Must

### F-009: コスパランキング
- **機能名:** コスパランキング（時間あたりコスト）
- **ユーザーストーリー:** 節約志向のゲーマーとして、各ゲームの「1時間あたりのコスト（円/時間）」を確認し、費用対効果を把握したい
- **受け入れ基準:**
  1. ユーザーが各ゲームの購入価格を手動入力できる（Steam APIでは購入価格の取得が困難なため）
  2. プレイ時間 / 購入価格 で「時間あたりコスト」を計算し、ランキング表示する
  3. コストの低い（コスパの良い）順にソートする
  4. 価格未入力のゲームはランキングから除外する
- **優先度:** Should

### F-010: ジャンル別集計
- **機能名:** ジャンル別プレイ時間集計
- **ユーザーストーリー:** ゲーマーとして、自分がどのジャンルのゲームに最も時間を費やしているか円グラフで把握したい
- **受け入れ基準:**
  1. Steam APIのジャンルタグ情報をもとにゲームをジャンル分類する
  2. ジャンル別の合計プレイ時間を円グラフ（またはドーナツチャート）で表示する
  3. ジャンルをクリックすると、そのジャンルに属するゲーム一覧を表示する
- **優先度:** Should

### F-011: 月別推移グラフ（手動ログ入力）
- **機能名:** 月別プレイ時間推移（手動ログ）
- **ユーザーストーリー:** ゲーマーとして、月ごとのプレイ時間推移を記録・確認し、自分のプレイ習慣の変化を追跡したい
- **受け入れ基準:**
  1. Steam APIでは月次プレイ時間を直接取得できないため、ユーザーが手動でログを入力する方式とする
  2. 月・ゲーム名・プレイ時間を入力するフォームを提供する
  3. 入力済みデータを折れ線グラフで月次推移として表示する
  4. 入力データはブラウザのローカルストレージ（未ログイン時）またはサーバー側DB（ログイン時）に保存する
- **優先度:** Should

### F-012: 友人比較モード
- **機能名:** 友人比較モード
- **ユーザーストーリー:** ゲーマーとして、友人のSteam IDと自分のデータを並べて比較し、プレイ傾向の違いを楽しみたい
- **受け入れ基準:**
  1. 2〜4人のSteam IDを入力して同時にデータを取得できる
  2. 各ユーザーの総プレイ時間・最多プレイゲームを横並びで比較表示する
  3. 共通してプレイしているゲームをハイライト表示する
- **優先度:** Should

### F-013: プレイ時間バッジ生成
- **機能名:** プレイ時間バッジ画像生成
- **ユーザーストーリー:** ゲーマーとして、「1000時間プレイヤー」などの称号バッジを画像として生成し、SNSアイコンやプロフィールに使いたい
- **受け入れ基準:**
  1. 総プレイ時間に応じた段階別の称号（例: 100h / 500h / 1000h / 5000h）を自動判定する
  2. 称号を含むバッジ画像を生成し、プレビュー表示する
  3. 生成したバッジ画像をPNG形式でダウンロードできる
- **優先度:** Should

### F-014: アカウント登録・ログイン
- **機能名:** ユーザーアカウント機能
- **ユーザーストーリー:** リピートユーザーとして、アカウントを作成してデータを保存し、過去の統計履歴を振り返りたい
- **受け入れ基準:**
  1. メールアドレスとパスワードによるアカウント登録・ログインができる
  2. ログイン状態でSteam IDを紐づけ、取得データをサーバー側に保存する
  3. 過去に取得した統計データの履歴を一覧表示できる
- **優先度:** Could

### F-015: 年間まとめレポート
- **機能名:** 年間まとめレポート自動生成
- **ユーザーストーリー:** ゲーマーとして、1年間のプレイ統計を「Spotify年間まとめ」風のビジュアルレポートとして閲覧・シェアしたい
- **受け入れ基準:**
  1. その年の総プレイ時間・最多プレイゲーム・ジャンル傾向などをカード形式でスライド表示する
  2. レポートをスクリーンショット可能な画像としてダウンロードできる
  3. レポートのSNSシェア用リンクを生成する
- **優先度:** Could

### F-016: ゲーム予算管理
- **機能名:** 月間ゲーム予算管理
- **ユーザーストーリー:** 予算管理したいゲーマーとして、月の課金上限を設定し、超過しそうな場合にアラートを受けたい
- **受け入れ基準:**
  1. 月間の予算上限額（円）を設定できる
  2. 購入記録を手動で入力し、累計額と残り予算を表示する
  3. 予算の80%を超えた時点で画面上に警告を表示する
- **優先度:** Could

### F-017: データキャッシュ
- **機能名:** Steam APIレスポンスキャッシュ
- **ユーザーストーリー:** ユーザーとして、同じSteam IDでの再検索時に高速にデータが表示されてほしい
- **受け入れ基準:**
  1. 同一Steam IDのリクエストに対し、一定時間内（15分〜1時間）はキャッシュ済みデータを返す
  2. キャッシュ利用時はその旨をUIに表示する（「XX分前のデータです」など）
  3. ユーザーが手動でキャッシュを無効化し最新データを取得できるリフレッシュボタンを提供する
- **優先度:** Must

### F-018: OGP画像生成
- **機能名:** シェア用OGP画像の動的生成
- **ユーザーストーリー:** ゲーマーとして、SNSでシェアしたときにプレイ統計のサムネイル画像が自動表示されてほしい
- **受け入れ基準:**
  1. 結果ページのURLをSNSに貼り付けた際、統計情報を含むOGP画像が表示される
  2. OGP画像にはプレイヤー名・総プレイ時間・最多プレイゲーム名を含む
  3. 画像は動的に生成される（ユーザーごとに異なる）
- **優先度:** Should

### F-019: プレイ時間TOP20ランキングの降順ソート保証
- **機能名:** TOP20ランキング降順ソート
- **ユーザーストーリー:** ゲーマーとして、プレイ時間TOP20の棒グラフがプレイ時間の多い順（降順）で正しく並んだ状態で閲覧したい
- **受け入れ基準:**
  1. TOP20棒グラフのバーがプレイ時間の降順（多い順）で上から下（または左から右）に並んでいること
  2. 同一プレイ時間のゲームが複数ある場合、ゲーム名のアルファベット順で安定してソートされること
  3. ソート順がページリロードやデータ再取得後も一貫して維持されること
  4. 20件未満のゲームしか所持していない場合でも、所持ゲーム全件が降順で正しく表示されること
- **優先度:** Must

### F-020: 日本語/英語 言語切り替え
- **機能名:** 日英言語切り替え機能
- **ユーザーストーリー:** 英語圏のゲーマーとして、UIを英語に切り替えてサービスを利用したい
- **受け入れ基準:**
  1. ヘッダーまたはフッターに言語切り替えボタン（日本語 / English）が常時表示されること
  2. 切り替えボタンを押すと、ページ全体のUIテキスト（ナビゲーション、ボタンラベル、説明文、エラーメッセージ、プレースホルダーなど）が選択した言語に即座に切り替わること
  3. 選択した言語設定がブラウザのローカルストレージに保存され、次回アクセス時に自動的に復元されること
  4. 言語切り替え時にページ遷移やデータの再取得が発生しないこと（クライアントサイドで完結すること）
- **優先度:** Must

### F-021: フロントエンド本番デプロイ対応
- **機能名:** Vercelへのフロントエンドデプロイ
- **ユーザーストーリー:** 運営者として、フロントエンド（React + Vite）をVercelにデプロイし、ユーザーが本番URLからサービスにアクセスできるようにしたい
- **受け入れ基準:**
  1. Vercel用の設定ファイルが用意され、Viteビルド成果物が静的サイトとしてホスティングされる構成になっていること
  2. SPAルーティングに対応し、どのURLパスでブラウザリロードしても404エラーにならず、正しいページが表示されること
  3. 本番ビルドが正常に完了し、ビルドエラーが発生しないこと
- **優先度:** Must

### F-022: バックエンド本番デプロイ対応
- **機能名:** Renderへのバックエンドデプロイ
- **ユーザーストーリー:** 運営者として、バックエンド（FastAPI）をRenderにデプロイし、本番環境でAPIが安定して稼働する状態にしたい
- **受け入れ基準:**
  1. Render用の設定ファイルが用意され、FastAPIアプリケーションがWeb Serviceとしてデプロイできる構成になっていること
  2. ヘルスチェックエンドポイントがRenderのヘルスチェック機構から正常応答を返すこと
  3. 環境変数（STEAM_API_KEY等）がデプロイ先のサービス設定で管理され、ソースコードにハードコードされていないこと
- **優先度:** Must

### F-023: 本番環境API通信・CORS対応
- **機能名:** 本番環境でのフロントエンド-バックエンド間API通信
- **ユーザーストーリー:** ユーザーとして、本番環境（Vercel上のフロントエンド）からバックエンドAPIを呼び出した際に、開発環境と同様にすべての機能が正常に動作してほしい
- **受け入れ基準:**
  1. バックエンドのCORS設定が本番フロントエンドのURLを許可し、ブラウザからのAPIリクエストがCORSエラーなく成功すること
  2. フロントエンドが参照するAPIのベースURLを環境変数で切り替えられ、開発時はlocalhostに、本番時はRenderのURLに自動的に向き先が変わること
  3. 本番環境でSteam ID入力からデータ取得・グラフ表示までの一連の操作が正常に完了すること
  4. CORS設定がワイルドカード（全許可）ではなく、許可するオリジンを明示的に指定していること
- **優先度:** Must

### F-024: 開発・本番環境の切り替え管理
- **機能名:** 環境別設定の管理
- **ユーザーストーリー:** 開発者として、開発環境と本番環境の設定（API URL、CORS許可オリジン等）を環境変数で一元管理し、コード変更なしで環境を切り替えたい
- **受け入れ基準:**
  1. フロントエンドの環境変数ファイル（開発用・本番用）が用意され、API接続先URLが環境ごとに設定されていること
  2. バックエンドの環境変数で、CORS許可オリジンやその他の環境依存設定が管理されていること
  3. 開発環境と本番環境それぞれで、コード変更なしに正しい設定が自動的に適用されること
- **優先度:** Must

---

## 3. スプリント計画

### Sprint 1: プロジェクト基盤構築
- **含まれる機能ID:** なし（基盤のみ）
- **完了定義:**
  - React + Vite のフロントエンドプロジェクトが起動し、トップページの空のルートが表示される
  - FastAPI のバックエンドプロジェクトが起動し、ヘルスチェックエンドポイントが応答する
  - Tailwind CSS が設定済みでスタイルが適用される
  - フロントエンドからバックエンドへの通信（CORS設定含む）が疎通する
  - 開発環境のホットリロードが動作する
- **依存スプリント:** なし

### Sprint 2: Steam API 連携
- **含まれる機能ID:** F-006, F-017
- **完了定義:**
  - バックエンドがSteam Web APIに接続し、GetOwnedGames / GetPlayerSummaries / ResolveVanityURL を呼び出せる
  - SteamID64とカスタムURLの両方でデータを取得できる
  - プロフィール非公開時に適切なエラーレスポンスを返す
  - APIレスポンスのキャッシュ機構が動作する
  - APIキーが環境変数で管理されフロントエンドに露出しない
- **依存スプリント:** Sprint 1

### Sprint 3: コア UI（入力・結果画面・サマリー）
- **含まれる機能ID:** F-001, F-004, F-007, F-008
- **完了定義:**
  - トップページにSteam ID入力フォームと「分析する」ボタンが表示される
  - 入力後、結果ページに遷移しサマリーカード（総ゲーム数・総プレイ時間・最多プレイゲーム・アバター）が表示される
  - ダークテーマが適用されている
  - モバイル・タブレット・デスクトップの各画面幅でレイアウトが適切に表示される
  - ローディング中・エラー時の表示が動作する
- **依存スプリント:** Sprint 2

### Sprint 4: グラフ表示（プレイ時間ランキング・直近2週間）
- **含まれる機能ID:** F-002, F-003
- **完了定義:**
  - 結果ページにプレイ時間TOP20の棒グラフが表示される
  - 直近2週間のプレイ状況が横棒グラフで表示される
  - グラフがモバイル画面でも横スクロールなしで閲覧できる
  - ゲームアイコンがグラフ内に表示される
  - 直近2週間のプレイがゼロの場合に適切なメッセージが表示される
- **依存スプリント:** Sprint 3

### Sprint 5: シェア機能
- **含まれる機能ID:** F-005, F-018
- **完了定義:**
  - 結果ページにTwitter/Xシェアボタンが配置される
  - シェアテキストに統計情報とサービスURLが含まれる
  - 結果ページURLのSNS貼り付け時にOGP画像がプレビュー表示される
  - OGP画像にプレイヤー名・統計情報が動的に含まれる
- **依存スプリント:** Sprint 4

### Sprint 6: コスパランキング
- **含まれる機能ID:** F-009
- **完了定義:**
  - 各ゲームに対して購入価格を手動入力できるフォームが表示される
  - 時間あたりコスト（円/時間）を計算し、コスパの良い順にランキング表示される
  - 価格未入力のゲームはランキングから除外される
  - ランキング結果がグラフまたはリスト形式で視覚的に表示される
- **依存スプリント:** Sprint 4

### Sprint 7: ジャンル別集計
- **含まれる機能ID:** F-010
- **完了定義:**
  - ジャンル別プレイ時間が円グラフまたはドーナツチャートで表示される
  - ジャンルクリックでそのジャンルのゲーム一覧が表示される
  - ジャンル情報が取得できないゲームは「その他」に分類される
- **依存スプリント:** Sprint 4

### Sprint 8: 手動ログ入力（月次推移代替）
- **含まれる機能ID:** F-011
- **完了定義:**
  - 月・ゲーム名・プレイ時間を入力するフォームが提供される
  - 入力済みデータが折れ線グラフで月次推移として表示される
  - 未ログイン時はローカルストレージにデータが保存される
  - Steam APIでは月次プレイ時間を直接取得できない旨の説明がUI上に表示される
- **依存スプリント:** Sprint 4

### Sprint 9: 友人比較モード
- **含まれる機能ID:** F-012
- **完了定義:**
  - 2〜4人分のSteam IDを入力するフォームが提供される
  - 各ユーザーの統計を横並びで比較表示できる
  - 共通プレイゲームがハイライト表示される
  - いずれかのユーザーのプロフィールが非公開の場合、該当ユーザーのみエラー表示される
- **依存スプリント:** Sprint 4

### Sprint 10: プレイ時間バッジ生成
- **含まれる機能ID:** F-013
- **完了定義:**
  - 総プレイ時間に応じた称号が自動判定される
  - バッジ画像がプレビュー表示される
  - バッジ画像をPNG形式でダウンロードできる
- **依存スプリント:** Sprint 4

### Sprint 11: アカウント登録・データ保存
- **含まれる機能ID:** F-014
- **完了定義:**
  - メールアドレス・パスワードによるアカウント登録・ログインが動作する
  - ログイン状態でSteam IDが紐づけられ、取得データがサーバー側に保存される
  - 過去の統計データ履歴が一覧表示される
  - 手動ログ入力データ（F-011）がログイン時はサーバー側に保存される
- **依存スプリント:** Sprint 3, Sprint 8

### Sprint 12: 年間まとめレポート・予算管理
- **含まれる機能ID:** F-015, F-016
- **完了定義:**
  - 年間プレイ統計をカード形式のスライドレポートとして表示できる
  - レポートを画像としてダウンロードできる
  - 月間予算上限の設定と購入記録の入力ができる
  - 予算80%超過時に警告が表示される
- **依存スプリント:** Sprint 11

### Sprint 13: ランキングソート修正・日英言語切り替え
- **含まれる機能ID:** F-019, F-020
- **完了定義:**
  - プレイ時間TOP20の棒グラフがプレイ時間の降順（多い順）で正しくソートされて表示される
  - 同一プレイ時間のゲームがある場合でもソート順が安定している
  - ヘッダーまたはフッターに言語切り替えボタン（日本語 / English）が表示される
  - 言語切り替えボタンを押すと、全ページの全UIテキストが選択した言語に切り替わる
  - 言語設定がローカルストレージに保存され、次回アクセス時に自動復元される
  - 言語切り替え時にページ遷移やデータの再取得が発生しない
- **依存スプリント:** Sprint 4

### Sprint 14: Vercel + Render 本番デプロイ対応
- **含まれる機能ID:** F-021, F-022, F-023, F-024
- **完了定義:**
  - Vercel用の設定ファイルが存在し、フロントエンドのビルドが正常に完了すること
  - Render用の設定ファイルが存在し、バックエンドのデプロイ構成が定義されていること
  - SPAルーティングが設定され、任意のURLパスでブラウザリロードしても404にならないこと
  - フロントエンドの環境変数ファイル（開発用・本番用）が用意され、APIベースURLが環境ごとに設定されていること
  - バックエンドのCORS設定が環境変数で管理され、本番フロントエンドのオリジンが許可されていること
  - CORS設定がワイルドカード全許可ではなく、明示的なオリジン指定になっていること
  - 開発環境（localhost）で既存の全機能が引き続き正常に動作すること（デプロイ対応による既存機能の退行がないこと）
- **依存スプリント:** Sprint 13（全既存機能が完成した状態で実施）

---

## 4. 非機能要件

### 4.1 パフォーマンス要件
- Steam ID入力から結果ページ表示までの所要時間は、キャッシュなしの場合でも5秒以内とする
- キャッシュ利用時は1秒以内にデータを返却する
- キャッシュの有効期限は15分〜1時間とし、同一Steam IDへの連続リクエストでSteam APIを再度呼び出さない
- フロントエンドの初回ロード（LCP）は3秒以内とする

### 4.2 セキュリティ要件
- Steam API Keyは必ずバックエンドの環境変数で管理し、フロントエンドのコードやレスポンスに含めない
- フロントエンドからSteam APIを直接呼び出さない（バックエンド経由のみ）
- ユーザー入力値（Steam ID / URL）に対するバリデーションとサニタイズを行う
- Phase 3のアカウント機能では、パスワードをハッシュ化して保存する
- HTTPS通信を必須とする

### 4.3 アクセシビリティ要件
- グラフにはalt属性または代替テキストによる数値情報を提供する
- キーボード操作で主要な操作（入力・検索・ナビゲーション）が完結する
- カラーコントラスト比はWCAG 2.1 AAレベル以上を確保する
- モバイル端末（iOS Safari / Android Chrome）で主要機能が動作する

### 4.4 スケーラビリティ要件
- Steam APIのレート制限（100,000リクエスト/日）を超えないよう、キャッシュ戦略で対応する
- 同時アクセス100ユーザー程度を想定した設計とする（MVP段階）
- バックエンドは無料枠のホスティング（Railway / Render）で動作する前提とする

### 4.5 エラーハンドリング要件
- プロフィール非公開時: 専用エラーメッセージと公開設定手順を案内する
- Steam API障害時: キャッシュ済みデータがあればそれを返し、なければ「一時的に利用できません」と表示する
- 不正なSteam ID入力時: 入力形式のガイダンスを表示する
- ネットワークエラー時: リトライボタンを提供する

---

## 5. 制約事項・前提条件

### 5.1 技術的制約
- **月次プレイ時間の取得不可:** Steam Web APIでは月別のプレイ時間履歴を直接取得できない。月次推移機能（F-011）はユーザーの手動ログ入力で代替する
- **購入価格の取得不可:** Steam Web APIではユーザーの購入価格を取得できない。コスパランキング（F-009）は手動入力で対応する
- **プロフィール公開依存:** ユーザーがSteamプロフィールを「公開」に設定していないとデータを取得できない

### 5.2 スコープ外（やらないこと）
- PSN / Nintendo Switch など他プラットフォームとの連携（将来的な検討事項として設計書に記載あり。本仕様のスコープ外）
- Steam以外の外部サービス（Discord等）との連携
- リアルタイムのプレイ時間トラッキング（常駐アプリのような仕組み）
- 日本語・英語以外の多言語対応（v1.1で日英切り替えをスコープに追加。3言語以上の対応は将来検討）
- ネイティブモバイルアプリ（Webアプリのレスポンシブ対応で代替）
- CI/CDパイプラインの構築（本スプリントではデプロイ設定ファイルの作成までをスコープとし、自動デプロイの構築は対象外）
- カスタムドメインの設定（Vercel / Render が提供するデフォルトドメインを使用する）

### 5.3 未解決事項（設計書のTODOより）
- [ ] サービス名の確定（SteamStats / Playlog / GameClock のいずれか、または別案）
- [ ] Steam API Keyの取得（開発用）
- [ ] OGP画像の具体的な生成方式の決定
- [ ] 利用規約・プライバシーポリシーの作成
- [ ] ドメインの取得

### 5.4 前提条件
- 開発者がSteam Web API Keyを事前に取得していること
- フロントエンドのデプロイ先としてVercelの無料枠が利用可能であること
- バックエンドのデプロイ先としてRenderの無料枠が利用可能であること
- ユーザーが自身のSteamプロフィールを「公開」に設定可能であること
- Vercelアカウントが作成済みで、プロジェクトの連携が可能であること（Sprint 14前提）
- Renderアカウントが作成済みで、Web Serviceの作成が可能であること（Sprint 14前提）

### 5.5 マネタイズ方針（参考）
| フェーズ | モデル | 内容 |
|---------|--------|------|
| MVP | 完全無料 | ユーザー獲得優先 |
| Phase 2 | フリーミアム | 無料: 基本機能 / Pro: コスパ計算・友人比較 |
| Phase 3 | サブスク | 月額300〜500円（予算管理・保存機能） |

---

*本仕様書は DESIGN_STEAM_TRACKER.md v1.0 をベースに作成。開発進行に合わせて随時更新する。*
